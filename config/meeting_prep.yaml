# Meeting prep configuration.

# Job source: "json" (bundled fictional export) or "browser" (scrape the
# FSM's completed-jobs page — the adapter path for systems with no API).
source: json
jobs: fixtures/fsm_jobs/jobs.json

# Browser source knobs.
jobs_url: "https://fsm.example/jobs/done"
profile: "trades-fsm"
selectors: {}            # override row/cell patterns here
